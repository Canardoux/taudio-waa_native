# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'taudio-waa_native' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for taudio-waa_native

  target 'taudio-waa_nativeTests' do
    # Pods for testing
  end
end
